###################################
# Shaping file for in-house IPs
###################################

["default"]
  replace_base = true
  max_deliveries_per_connection = 10
#  enable_tls = 'OpportunisticInsecure'
  enable_tls = 'Disabled'

["not-yahoo.aasland.com"]
  max_deliveries_per_connection = 10000
  enable_tls = 'Disabled'
  max_ready = 100000
  connection_limit = 40000

